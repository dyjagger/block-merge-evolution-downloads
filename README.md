# Block Merge Evolution — Android builds

This repository hosts signed Android test builds for Block Merge Evolution.

Use the public installer page for the current download and Android installation
instructions. Release assets are published here so the APK can be downloaded
from any internet connection.
